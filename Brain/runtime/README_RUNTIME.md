# Runtime data

Robot Brain creates profile-owned memory, document indexes, uploaded voice references, generated audio and logs in this folder while it runs.

These generated files are deliberately excluded from the clean release. Back up `runtime/<profile>/` separately when moving an existing robot's memories or private document library to another installation.
